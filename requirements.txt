langgraph==1.2.9
pydantic>=2
pydantic-settings>=2
langgraph-checkpoint-sqlite==3.1.0
requests>=2.31
keyring>=25
click>=8.1
gradio>=4.0
langchain-groq==1.1.2
langchain==1.3.14
chromadb==1.5.8
langsmith>=0.7
langfuse>=4.13,<5
requests-cache>=1.2
# ragas 0.4.x keeps the single_turn_score API used by eval_data/ragas_eval.py.
# langchain-community must stay <0.4.2: ragas imports
# langchain_community.chat_models.vertexai at startup, and 0.4.2 removed that
# module (the app then misreports it as "ragas not installed").
ragas>=0.4,<0.5
langchain-community>=0.4.1,<0.4.2
litellm
